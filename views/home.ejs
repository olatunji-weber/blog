<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p><%= startingString %></p><hr>

    <% postString.forEach(function(post){ %>
        <h1> <%= post.title %> </h1>
        <p> <%= post.body.substring(0, 300) + "..."%>   <a href="/posts/<%= post.title %>">Read More</a> </p>
        <!-- <p> <%= post.body%>   <a href="/posts/<%= post.title %>">Read This</a> </p> -->
       
    <% }); %>

<%- include("partials/footer"); -%>
